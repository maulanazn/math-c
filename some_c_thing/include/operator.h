#include <stdio.h>
#include <stdlib.h>

double add(double firstnum, double secondnum);
double dim(double firstnum, double secondnum);
double times(double firstnum, double secondnum);
double divide(double firstnum, double secondnum);
void operate();
